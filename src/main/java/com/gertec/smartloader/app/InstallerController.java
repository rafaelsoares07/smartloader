package com.gertec.smartloader.app;

import javafx.fxml.FXML;
import org.springframework.context.annotation.Scope;
import org.springframework.stereotype.Component;

/**
 * Controller for the Installer view.
 *
 * <p>Increment 0: a simple placeholder. The navigation item is fully functional; the
 * real installer behaviour arrives in a future increment.</p>
 */
@Component
@Scope("prototype")
public class InstallerController {

    @FXML
    private void initialize() {
        // TODO (future increment): implement the installer screen.
    }
}
